Para generar una cadena repitiendo un símbolo, puedes usar la función [`repeat`](https://kotlinlang.org/api/latest/jvm/stdlib/kotlin.text/repeat.html):
```kotlin
"@".repeat(4)
```
Esto generará la siguiente cadena: `@@@@`.